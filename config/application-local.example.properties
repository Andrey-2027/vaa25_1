# Copy this file to application-local.properties and set local values.
# application-local.properties is ignored by Git and is not packaged in the JAR.

spring.datasource.url=jdbc:postgresql://localhost:5432/DBVaa25
spring.datasource.username=postgres
spring.datasource.password=

# Optional local report template directories.
ureport.fileStoreDir=ureport-files
jrxml.fileStoreDir=jrxml-files

# Optional one-off Hibernate diagnostics (query/entity/collection/session counters).
# spring.jpa.properties.hibernate.generate_statistics=true
