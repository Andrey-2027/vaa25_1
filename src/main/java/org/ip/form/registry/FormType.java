package org.ip.form.registry;

/**
 * Типы форм в системе.
 */
public enum FormType {
    LIST,      // ListForm — форма списка
    ITEM,      // ItemForm — форма элемента (создание/редактирование)
    SELECTION  // SelectionForm — форма выбора (lookup)
}
