package org.ipro.events;

/**
 * Канал, из которого инициирована операция над сущностью.
 *
 * <p>Значения не зависят от Vaadin или конкретного приложения и могут
 * использоваться одинаково для UI, интеграций, импорта и фоновых задач.</p>
 */
public enum EventSource {
    UI,
    REST,
    IMPORT,
    JOB,
    SYSTEM
}
