package org.ipro.metadata;

/**
 * Сущность с человекочитаемым отображаемым именем.
 *
 * <p>Платформенный контракт нижнего слоя: используется рендерами значений,
 * autocomplete-полями ссылок и отчётным движком для подписей entity-ref.
 * Прикладные сущности реализуют его напрямую; доменная логика того, что
 * считать «именем», остаётся на стороне приложения.</p>
 */
public interface HasDisplayName {
    String getDisplayName();
}
