package org.ipro.telemetry.core;

/**
 * Запись об одном SQL-стейтменте, собранная за время L2-трассировки.
 * Параметры bind Hibernate 7 не отдаёт без внешней библиотеки — только
 * текст и время выполнения. Коллекция ведётся лениво (только для
 * трассируемых операций), в штатном режиме объекты не создаются.
 */
public record SqlRecord(String sql, long executionNanos) {
}